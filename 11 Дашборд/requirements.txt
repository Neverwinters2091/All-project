Библиотеки устанавливаемые этими командами нужны для отработки дашборда:

1) python -m pip install dash==1.6.1
2) python -m pip install sqlalchemy
3) python -m pip install psycopg2
4) python -m pip install pandas
5) python -m pip install plotly

На локальной машине был установлен Python 3.8	